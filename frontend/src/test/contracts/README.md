# API contract fixtures

No patient, appointment, slot, ETA, queue, consultation, registration, check-in, or walk-in API contract is validated on this branch. This directory intentionally contains no invented endpoint fixtures; fixtures will be added only when an authoritative versioned contract is available.
