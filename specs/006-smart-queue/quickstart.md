# Smart Queue Validation Quickstart

Prerequisites: queue-entry, scheduling slot-duration, branch timezone, audit, and trusted staff capabilities.

Run focused queue tests first, then `mvn test`. Validate priority ordering, patients ahead, unavailable estimates, scoped non-disclosure, and concurrent recalculation.
